<?php
/**
 * Copy this file to local-secrets.php (git-ignored) and fill in real keys
 * for local/dev testing. On a real site, prefer entering keys through
 * WP Admin > WP Edit With AI > Settings instead — those are stored per-site
 * in wp_options and take priority over these constants.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

define( 'WP_EDIT_WITH_AI_GEMINI_KEY_1', '' );
define( 'WP_EDIT_WITH_AI_GEMINI_KEY_2', '' );
define( 'WP_EDIT_WITH_AI_KIE_API_KEY', '' );
